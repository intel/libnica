language: c
os: linux
compiler:
    - clang
    - gcc

addons
    apt:
        packages:
            - check
            - valgrind

before_script:
    - ./autogen.sh

script:
    - ./configure && make && make check
